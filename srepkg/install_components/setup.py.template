from distutils.core import setup
import setuptools


setup(
    name='$pkg_name_srepkg',
    description='Repackaged by fsrpkg to ensure app runs in isolated env',
    install_requires=[],
    packages=setuptools.find_packages(),
    package_data={"": ["*.*"]},
    include_package_data=True,
    zip_safe=False
)