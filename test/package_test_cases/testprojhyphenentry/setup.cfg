[metadata]
name = testprojhyphenentry

[options]
package_dir=
    =src
packages = find:
install_requires =
    numpy

[options.packages.find]
where = src

[options.entry_points]
console_scripts =
    my-123-multiplier = testprojhyphenentry.app:run