[metadata]
name = testproj

[options]
package_dir=
    =src
packages = find:
install_requires =
    numpy

[options.packages.find]
where = src

[options.entry_points]
console_scripts =
    my_123_multiplier = testproj.app:run