[metadata]
srepkg_name = testprojsrepkg
dist_dir = dummy_dist_dir
sdist_src = dummy_sdist_src

[options]
package_dir=
    =src
packages = find:
install_requires =
    numpy

[options.packages.find]
where = src

[options.entry_points]
console_scripts =
    my_123_multiplier = testproj.app:run